import { Company } from "../models/company";
import { Job } from "../models/job";

// need to write controller function

function create(req, res) {
  // set the company of the user here?
}


export {
  create
}